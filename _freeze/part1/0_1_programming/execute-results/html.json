{
  "hash": "d55b5f6805f84ac00a0387beba95958b",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"프로그래밍 기본 문법\"\nauthor: \"Eunji Ha\"\ndate: last-modified\n---\n\n## Why 프로그래밍 문법?\n\nR 기초 문법(`dplyr`, `ggplot2`)만으로도 분석은 가능합니다.\\\n하지만 **반복 작업 자동화**와 **AI가 생성하는 코드 이해**를 위해 프로그래밍 기본 구조를 알아야 합니다.\n\n::: callout-tip\n## 바이브코딩과의 연결\n\nClaude는 코드를 짤 때 `for`, `if`, 함수를 적극적으로 사용합니다.\\\n이 구조를 이해하면 AI가 만든 코드를 **읽고, 수정하고, 디버깅**할 수 있습니다.\n:::\n\n## 1. 조건문: `if` / `else if` / `else`\n\n조건에 따라 다른 코드를 실행합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 기본 구조\nif (조건) {\n  # 조건이 TRUE일 때 실행\n} else {\n  # 조건이 FALSE일 때 실행\n}\n```\n:::\n\n\n**예제: p-value에 따라 유의성 판단**\n\n\n::: {.cell}\n\n```{.r .cell-code}\np_value <- 0.03\n\nif (p_value < 0.001) {\n  sig <- \"***\"\n} else if (p_value < 0.01) {\n  sig <- \"**\"\n} else if (p_value < 0.05) {\n  sig <- \"*\"\n} else {\n  sig <- \"ns\"\n}\n\nprint(sig)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"*\"\n```\n\n\n:::\n:::\n\n\n::: callout-note\n## `ifelse()` — 벡터 전체에 조건 적용\n\n`if`는 값 1개, `ifelse()`는 벡터 전체에 작동합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf$age_group <- ifelse(df$age >= 65, \"elderly\", \"non-elderly\")\n```\n:::\n\n\n`dplyr::case_when()`도 같은 용도이며, 조건이 3개 이상이면 더 깔끔합니다.\n:::\n\n## 2. 반복문: `for`\n\n같은 작업을 여러 번 반복합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfor (변수 in 벡터) {\n  # 반복 실행할 코드\n}\n```\n:::\n\n\n**예제: 여러 변수에 대해 t-test 반복**\n\n\n::: {.cell}\n\n```{.r .cell-code}\nvars <- c(\"BMI\", \"SBP\", \"DBP\", \"HbA1c\")\nresults <- list()\n\nfor (v in vars) {\n  formula <- as.formula(paste(v, \"~ group\"))\n  results[[v]] <- t.test(formula, data = df)\n}\n\n# 결과 확인\nresults[[\"BMI\"]]\n```\n:::\n\n\n::: callout-tip\n## 실전 패턴: 여러 파일 한번에 읽기\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfiles <- list.files(\"rawdata/\", pattern = \"\\\\.csv$\", full.names = TRUE)\ndf_list <- list()\n\nfor (f in files) {\n  df_list[[f]] <- read.csv(f)\n}\n\ndf_all <- do.call(rbind, df_list)\n```\n:::\n\n:::\n\n## 3. 반복문: `while`\n\n조건이 TRUE인 동안 반복합니다. `for`보다 사용 빈도가 낮지만, AI 생성 코드에서 종종 등장합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nwhile (조건) {\n  # 반복 실행\n  # ⚠️ 조건을 FALSE로 만드는 코드가 반드시 있어야 함 (무한루프 방지)\n}\n```\n:::\n\n\n**예제: 수렴할 때까지 반복 (iteration)**\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndiff <- Inf\ntol <- 1e-6\nprev <- 0\niter <- 0\n\nwhile (diff > tol) {\n  current <- some_update_function(prev)\n  diff <- abs(current - prev)\n  prev <- current\n  iter <- iter + 1\n}\n\ncat(\"Converged after\", iter, \"iterations\\n\")\n```\n:::\n\n\n## 정리\n\n| 문법      | 용도               | 의학연구 활용 예시               |\n|-----------|--------------------|----------------------------------|\n| `if/else` | 조건 분기          | p-value 유의성 표시, 이상치 처리 |\n| `for`     | 정해진 횟수 반복   | 여러 변수 분석, 파일 일괄 처리   |\n| `while`   | 조건 충족까지 반복 | 수렴 알고리즘, AI 생성 코드 이해 |\n\n::: callout-important\n## 다음 장에서는\n\n이 문법들을 **함수(function)**로 묶어서 재사용하는 법을 배웁니다.\\\n→ 이것이 AI가 코드를 구조화하는 핵심 방식입니다.\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}